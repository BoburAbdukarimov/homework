import ReactDOM from 'react-dom/client';
// eslint-disable-next-line no-unused-vars
import React from 'react';
import App from './App';




//React.createEleent() wu bn ishlaydi
ReactDOM
    .createRoot(document.getElementById('root'))
    .render(<App />);
